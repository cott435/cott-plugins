# Evals

Ad hoc tests run against this plugin's own skills and agents — not the automated
skill-creator benchmark loop (that produces its own workspace under a skill's own
`<skill-name>-workspace/`), but targeted checks of one specific behavioral claim: does an
agent actually invoke a skill when it should, does a patched instruction change behavior, does
a description trigger correctly, and so on.

## Convention

The convention is the `log-eval` skill in the `plugin-dev` plugin, shared by every plugin
repo: one file per test run named `<date>-<subject>.md`, the required **Tested against**
commit and model fields, what each section must contain, and the rule that the entry is
written before results are reported back. Read it before adding an entry.

Two things it insists on, because they are the ones that get skipped:

- A clean pass is recorded exactly like a failure. The record is about whether the claim was
  ever checked, not about whether the news was good.
- The commit SHA is the commit of the file(s) under test *as of when the test ran*
  (`git log -1 --format=%h -- <path>`), and the model is what actually ran it — `inherit`
  resolves to something specific, and that is what the result is about.

## Index

| Date | Subject | File | Commit | Verdict |
|---|---|---|---|---|
| 2026-09-16 | `implementer` — does it actually *invoke* `security-review` (not just reason about it from memory) when a section matches its When to Activate conditions | [2026-09-16-implementer-security-review-trigger.md](2026-09-16-implementer-security-review-trigger.md) | uncommitted at test time | Gap found (2/4 positive cases actually invoked the tool) and fixed by rewriting the agent's Security step; re-run confirmed 4/4 |
| 2026-09-17 | `documenter` — does every `interface.md` heading it is told to expect exist in the template the implementer owns | [2026-09-17-documenter-interface-heading-contract.md](2026-09-17-documenter-interface-heading-contract.md) | `259785a` (fix uncommitted) | Claim fails on 2 of 7 headings (`Scripts`, `Consumers`) — never written anywhere in the plugin; fixed in `documenter.md`. Mechanical check only: no agent run was possible (`claude` CLI disabled in the session), so the behavioral impact stays unverified |
| 2026-09-17 | cross-file contract sweep — headings a reader parses, the `scripts/` prohibition, the `docs/api/<pkg>.md` writer | [2026-09-17-cross-file-contract-sweep.md](2026-09-17-cross-file-contract-sweep.md) | `e4bedca` / `259785a` / `0604620` (fixes uncommitted) | 2 of 3 claims false before (4 stale `scripts` mentions, 2 wrong `docs/api` attributions), all 3 pass after. Heading check passed before, confirming the 0.2.1 documenter fix holds. Mechanical: no agent run was possible |
| 2026-09-18 | probe doc headings — is every heading the four readers of a probe doc name one the template actually defines, after `probe-source` split into `api` and `dataset` kinds | [2026-09-18-probe-doc-headings-claim.md](2026-09-18-probe-doc-headings-claim.md) | `d23232c` (change uncommitted) | Claim fails on its own owner while the template is a fenced `##` block — `check_headings` only reads numbered bolded items — and passes once the two templates move to `planning-templates/references/source-probe.md` in that form. 9/9 claims pass. Mechanical: no agent run |
| 2026-09-18 | platform facts for 0.5 (evals A, B) — which `subagent_type` form a plugin skill's Agent call resolves, and whether `${CLAUDE_PLUGIN_ROOT}` is substituted in a skill body and an agent body | [2026-09-18-platform-facts.md](2026-09-18-platform-facts.md) | `3127526` | Both hold: `dev-team:<agent>` resolves, bare `reviewer` errors ("Agent type 'reviewer' not found"); the variable is substituted to an absolute path in both bodies. Real headless runs, `claude-sonnet-5`, Claude Code 2.1.270 |
| 2026-09-18 | vendored skills (0.5 eval C) — no `agent-skills` skill adapted in phase 1 still carries JS-stack text or `CONSTRAINTS.md` | [2026-09-18-c-vendored-skills-js-stack-grep.md](2026-09-18-c-vendored-skills-js-stack-grep.md) | uncommitted at test time (phase 1 commit) | Holds: 0 hits in all three adapted files; positive control hits 3–25 lines per upstream original. Mechanical; re-run over the fourth file in phase 6 |
| 2026-09-18 | commit per run (0.5 eval D) — `status.py` review freshness is commit-based; each run commits exactly what it wrote, with the `Dev-Team-Run:` trailer | [2026-09-18-d-commit-per-run.md](2026-09-18-d-commit-per-run.md) | uncommitted at test time (phase 2 commit) | Mechanical holds (7 states). Behavioral failed first: the architect never committed and a spawned researcher did. Fixed by adding commit-then-return to each forked skill's last step and a `Commit: yes` marker for direct probes; the re-run holds for `plan-repo` and `implement-section`. `plan-package` did not complete headless: designers were backgrounded, a pre-existing bug filed separately. Its commit was made by hand |
| 2026-09-18 | foreground fan-out (0.5 eval D follow-up) — does a forked architect get its designers back in the same turn and finish `plan-package` headless | [2026-09-18-d2-foreground-fanout.md](2026-09-18-d2-foreground-fanout.md) | `9fc74a3` (fix uncommitted, phase 2) | Failed first even with the `run_in_background: false` Hard rule: bare `agent: architect` silently forks as general-purpose, so the architect prompt never loads. With `agent: dev-team:<name>` on all 12 skills plus the flag, it holds: 3 designers in one message run in parallel, and the fork writes integration + surface and commits (`f5c7393`). Main-thread tool calls 0. Real headless runs, `claude-sonnet-5` |
| 2026-09-18 | reviewer order of authority, recorded-deviation rule, `sync-design` (0.5 eval E) — are the reviewer's and implementer's authority lists the same; does the reviewer grade a recorded deviation WARNING and an unrecorded one CRITICAL; does `sync-design` append one As shipped row | [2026-09-18-e-order-of-authority-sync-design.md](2026-09-18-e-order-of-authority-sync-design.md) | uncommitted at test time (phase 3 commit) | Holds. The lists are equal. `check-contracts` catches a renamed item but not a swapped backticked path, which the eval script catches. Clean run: 1 WARNING naming sync-design, 1 CRITICAL. `sync-design` wrote 1 row, 0 lines removed, one commit with the trailer. The first run's second CRITICAL came from a seeding error (a listed test the code could not pass). The second run is discarded: its repo was cloned from the first. Real headless runs, `claude-sonnet-5`, $1.76 |
| 2026-09-18 | tester and `test-section` (0.5 eval F) — the tester never reads section source, its intent tests are red, it commits only its tree, and reconcile edits only the tests citing a recorded deviation | [2026-09-18-f-tester-intent-reconcile.md](2026-09-18-f-tester-intent-reconcile.md) | uncommitted at test time (phase 4 commit) | Holds, 4/4. 23 intent tests, 23 red, commit = `tests/intent/ingest/` + one fixture. The implementer ran them before writing code and returned `Intent tests: 23/23`. Reconcile on one seeded deviation changed exactly 2 functions, both citing design §3. `status.py`'s intent column left bytecode that the next run read as uncommitted; fixed and re-run. Watch: the tester lists files with `find`, reconcile checked the baseline after editing. Real headless runs, `claude-sonnet-5`, $1.56 |
| 2026-09-18 | review-plan, re-plan, plan gate (0.5 eval G) — does `review-plan` find exactly two seeded plan defects; does the `plan-package` re-run re-delegate only the affected section and tick the follow-ups; does the second review approve and `--plan-gate` pass; does the implementer block on an open plan finding | [2026-09-18-g-review-plan-replan-gate.md](2026-09-18-g-review-plan-replan-gate.md) | uncommitted at test time (phase 5 commit) | Holds after three fixes, each re-run. 2/2 CRITICALs, 2 follow-ups; the implementer blocked. The first re-plan patched a design defect in `integration.md` with 0 designers; fixed with a "design defect → revise that design" rule, and the re-run re-delegated exactly `clean`. The first gate after approval failed on a same-day `-2` report; `status.py` now ranks suffixes. A real `plan-package` dropped 6 designer OQs; the rule is now "a `D` or a named integration resolution". 10 mechanical gate cases pass. Real headless runs, `claude-sonnet-5`, $5.84 |
| 2026-09-19 | constraints gate and axis 0 (0.5 eval H) — does `status.py --gate` print exactly one `constraint … FAIL` line for a coverage floor above real coverage; does the reviewer raise exactly one `bar lowered` CRITICAL for one new `# type: ignore` in the review diff | [2026-09-19-h-constraints-gate-bar-lowered.md](2026-09-19-h-constraints-gate-bar-lowered.md) | uncommitted at test time (phase 6 commit) | Holds. Floor 99 vs real 94 %: one constraint line, tree clean; floor 80: none. The review had 1 `bar lowered` at the seeded line, and the older ignore outside the diff was not flagged; 2 more CRITICALs were real bugs in the seed. Setup found two broken template commands, `--cov=<pkg>` (0 % beside a root `data/`) and `interrogate` without `-I -i`, both fixed first. Watch: the reviewer ran 7 of 8 rows (skipped `ruff format --check`). Eval C re-run (fourth file) and eval E script pass; 21/21 contracts plus 2 planted controls. Real headless run, `claude-sonnet-5`, $0.52 |
| 2026-09-19 | spine-first planning (0.5 eval I) — does the first `plan-package` design only the spine and write no `surface.md`; does a run before the spine is built write nothing; does the completion run spawn two designers with `Sibling shipped:`; does `--all` design everything at once | [2026-09-19-i-spine-first-planning.md](2026-09-19-i-spine-first-planning.md) | uncommitted at test time (phase 7 commit) | Holds 4/4 after one fix. Spine `ingest` (2 of 2) designed alone; too-early wrote nothing; completion: 2 designers, both `Sibling shipped: …/ingest/README.md`, `surface.md` written. `--all` first ran as a spine run: substitution garbled the flag sentence. Fixed, and the re-run designed 3. `review-plan` blocker and `status.py` spine line hold; contracts 21/21 plus a planted control. Real headless runs, `claude-sonnet-5`, $11.18 |
| 2026-09-19 | `run-package` driver (0.5 eval J) — does the driver run tester → implementer → tester → reviewer per section in dependency order and then the close-out; does a spine plan build, complete, review and stop; does an unfixable CRITICAL stop at implementer run 3; does the driver duplicate no skill text | [2026-09-19-j-run-package-driver.md](2026-09-19-j-run-package-driver.md) | uncommitted at test time (phase 8 commit) | Holds after six fixes, five re-run. Spine: `ingest` built with one review round-trip, plan completed and approved, stop. Full: `clean`, `storage`, finalize, package review, then the `sync-design` architect called the Skill tool, was refused, and blocked. The spawn block now says Read, and the resume ran sync-design and ended `done`. (iii): real seed blocked at run 1; stub agents stopped at run 3 with the right `next`. Fixed along the way: step 3.1 skipped, the run gate after finalize, xfail read as failing, wrapped follow-ups, and ruff formatting `docs/`. 0 shared sentences; 22/22 contracts plus a planted control. Real headless runs, `claude-sonnet-5`, $26.94 |
| 2026-09-19 | end-to-end (0.5 eval K) — does the whole new-repo workflow build the two-package fixture under 0.5: both packages shipped, every section and plan reviewed, zero open review follow-ups, tests, import contracts, strict docs and constraints passing, one trailered commit per run, every design synced | [2026-09-19-k-end-to-end.md](2026-09-19-k-end-to-end.md) | `9d1c048` + uncommitted phase-9 docs | Partial. 11 commands ran unaided: a correct stop for decisions and one for a plan CRITICAL, both packages shipped, 398 rows stored idempotently, 32 trailered commits, 5/5 designs synced, lint-imports, mkdocs strict and constraints pass. Four checks fail on plugin defects, not fixed here: the plan reads stale after `sync-design`; a package review approved with a CRITICAL, so 1 review follow-up stays open; root `pytest` collides on two `tests` packages (per package 109 + 54 pass); run 5's architect spawned `general-purpose` designers (bare `designer` in `architect.md`). Real headless runs, `claude-sonnet-5`, $35.50 |
| 2026-09-19 | fixed cost per agent run (0.5 eval L) — tokens each driver-spawned agent holds before any work, and at the end, from eval K's streams | [2026-09-19-l-fixed-cost-per-section.md](2026-09-19-l-fixed-cost-per-section.md) | `9d1c048` + uncommitted phase-9 docs | Measured, 33 spawns. Fixed cost is constant per role (spread < 350 tokens): implementer ~54k, tester ~40k, reviewer and architect ~38k, designer ~26k. That is 42–60 % of the context each ends with, so a derived task packet can only cut the later reads (16k–130k). No change in 0.5. No extra cost |
| 2026-09-20 | the four defects eval K found (0.5 phase 9 fix pass) — plan freshness after `sync-design`, a CRITICAL under `approve with fixes`, root `pytest` colliding on two `tests` trees, and the architect spawning bare `designer` | [2026-09-20-m-k-defect-fixes.md](2026-09-20-m-k-defect-fixes.md) | `8d1c722` (fixes uncommitted) | All four hold. 8 mechanical plan-freshness cases pass, including the negative controls (a plain design edit and a contract edit still go stale). The re-review that said `approve with fixes` on an unfixed CRITICAL now says `request changes`. Root `pytest` 16 errors → 163 passed with the template's `addopts`, per package still green. The `plan-package` fan-out spawns two `dev-team:designer`; 23/23 contracts with the new claim, which fails on the planted bare name. Real headless runs, `claude-sonnet-5`, $2.07 |
| 2026-09-20 | the tester owns its tree, and no agent sweeps the machine (0.5.1) — does the reviewer address a `tests/intent/` finding to `<pkg>/<section>/intent`, does reconcile clear it, does a fresh intent run write no suppression, and does any agent's shell leave the repo | [2026-09-20-n-intent-target-and-shell-scope.md](2026-09-20-n-intent-target-and-shell-scope.md) | `b04aa9b` (fixes uncommitted) | Both hold. The review filed to the new target and the next reconcile cleared and ticked it, rewriting one suppression as `cast` and refiling the 11 it could not clear after checking that repo's own lint config. A fresh intent run wrote 13 tests and 0 suppressions (one `xfail` cites `D1`). With the new per-file ignores, K's 11 `# noqa` comments become unnecessary (11 errors → clean) while `src` still errors. Gate and column cases pass. 119 Bash calls across 4 runs, 0 sweeps — weak as a negative, since K's sweep was 1 in 8 runs; 24/24 contracts plus a planted `find /`. Real headless runs, `claude-sonnet-5`, $5.41 |
| 2026-09-20 | `implementer` — the 2026-09-16 security-review claim rerun from a committed set, plus the first eval sets for `documenter` and `researcher` | [2026-09-20-implementer-security-review-sets.md](2026-09-20-implementer-security-review-sets.md) | `9fc74a3` | Claim holds: 8/8 cases agree with the post-fix verdict (87.5% vs 40.6% against no agent), on a proxy run — no `Skill` call is observable, so "invoked" means the checklist file was opened. New finding: on three non-matching sections the agent declined the checklist and then wrote a security paragraph anyway, the same unearned-paragraph failure step 3 names |
| 2026-09-22 | plan-loop exit (eval O) — does `status.py --plan-rounds` count consecutive `request changes` plan reviews; does a round-2 `review-plan` classify a half-propagated fact as one unfixed finding, write `Round:`/`Convergence:`, return `Loop: stopped` and the two-command block; does `--defer` move the standing findings to their sections and approve with fixes; does the new forbid claim fail on a planted bare next command | [2026-09-22-o-plan-loop-exit.md](2026-09-22-o-plan-loop-exit.md) | `ca31fb3` (fixes uncommitted, branch `plan-loop-exit`) | Holds, 13/13: 9 mechanical round-count and gate cases, the forbid claim fails on the planted sentence, the round-2 review says `Round: 2`, `Convergence: 1 prior unfixed, 0 new`, one *incomplete propagation* CRITICAL, `Loop: stopped` and the two-command block, and `--defer` moves it to `data/clean`, approves with fixes, and the gate passes. Watch: O1 put a sentence before `Result:` (rule tightened; O2 clean); O2 omitted `Loop:`. Architect side untested. Real headless runs, `claude-sonnet-5`, $0.67 |
| 2026-09-22 | section-loop exit and closed CRITICAL list (eval P) — does `status.py --rounds` count section and surface rounds; does a round-2 `review-section` verify the prior CRITICAL fixed, grade a docstring and a name WARNING, downgrade a wrong result outside the diff to a `— noted` follow-up, return `Loop:` as line 3 and continue when the prior finding is fixed; does the widened forbid claim fail on a planted bare next command | [2026-09-22-p-section-loop-exit.md](2026-09-22-p-section-loop-exit.md) | `5db17e5` (fixes uncommitted, branch `section-loop-exit`) | Holds after two fixes: the first run dropped `Loop:` again (now a fenced three-line template) and the round-2 "as many new as before" test stopped a healthy loop (now: round 2 stops only on a prior finding unfixed, round 3 always); the re-run had `Result:`/`Verdict:`/`Loop: converging` as its first three lines, `Convergence: 0 prior unfixed, 0 new`, the planted wrong result as WARNING + `— noted`, `approve with fixes`. 7 mechanical cases and the forbid probe pass. Watch: the two runs graded the same defect kind 1 and kind 3. Real headless runs, `claude-sonnet-5`, $0.87 |
