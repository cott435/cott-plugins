# 07 — end to end, docs, release proposal

Phase 7. Reruns every eval set on the finished branch, brings the docs in line with what the
branch built, and proposes 0.9.0 in chat. Nothing new is designed here; a failure found by
the end-to-end run is fixed in the file that owns it, or becomes a Deviation.

## Decisions

- **Release level: minor (0.9.0).** A new skill and new behavior in two typed skills; notes
  written before 0.9 still run, so nothing a user already relies on breaks.
- **The end-to-end run measures, it does not optimize:** trigger sets run with `run_eval`
  only; no description changes in this phase.

## Files

| Path | Change |
|---|---|
| `README.md` | **Workflows**: each of the three paragraphs names `run-evals` where evals run; the closing "What is the same in all three" gains "evals run through `run-evals` from committed sets in `evals/sets/`" |
| `site/workflows/small-change.md` | the eval step: `run-evals` on the target's set (add the test to the set first) |
| `site/workflows/phased-change.md` | Chats 1…N: the Evals table, `run-evals`, the review stop |
| `site/workflows/new-plugin.md` | phases' evals and sets written in phase 0 |
| `CLAUDE.md` (plugin-dev) | + bullet under **This plugin in particular** (below) |
| `CHANGELOG.md` | `## [Unreleased]` with Added / Changed and the overview's breaking-changes list |

## Specification

`CLAUDE.md` bullet, verbatim:

> - **`run-evals` is shared.** Its `references/eval-kinds.md` is the one list of eval kinds;
>   `plan-phases` writes notes against it and `run-phase` runs them through it. A kind added
>   or renamed there is a change to all three, and `check-contracts`' eval-kinds claim fails
>   until they agree. Eval sets under `evals/sets/` are committed; `evals/workspace/` never is.

## Steps

1. Docs and CLAUDE.md; `check-contracts`; `build-site`.
2. End-to-end evals below, in one iteration per target, reviewed together in the viewer.
3. CHANGELOG from the phases' ledger rows and eval logs.
4. Commit: `plugin-dev 0.9-evals (phase 7): end-to-end evals, docs, 0.9.0 proposed`.
5. In chat: the end-to-end table, and "This looks like a **minor** bump to 0.9.0 — run
   `bump-version`?" Stop. Never bump or tag here.

### Evals

| ID | Kind | Target | Baseline | Set evals | Pass bar |
|---|---|---|---|---|---|
| P7-M1 | mechanical | plugin-dev | — | — | `check-contracts` all PASS; `validate` exits 0 on every `evals/sets/*.json` and `*.trigger.json` in plugin-dev and dev-team. |
| P7-L1 | load | plugin-dev | — | — | `claude --plugin-dir ./plugin-dev -p "List the skills you have from plugin-dev"` names all eight. |
| P7-B1 | behavioral | `plan-phases` | `plugin-dev-v0.8.0` | `evals/sets/plan-phases.json` 1–4 | ≥ 90% `with_skill`; eval 4 all pass. |
| P7-B2 | behavioral | `run-phase` | `plugin-dev-v0.8.0` | `evals/sets/run-phase.json` 1, 2 | All pass. |
| P7-T1 | trigger | the five automatic skills | — | `*.trigger.json` | Each held-out accuracy within 0.05 of its phase-2 result. |

## Done when

- Every row above logged; the ledger's last row `done`; the bump proposed in chat and not
  run.
