# plugin-dev 0.9-evals — progress

Branch `plugin-dev-0.9-evals`. One row per phase of `0.9-evals-00-overview.md`. `run-phase`
reads this file first, does the first row that is not `done`, and fills the row in the same
commit as the phase. The Commit cell holds `(phase N)` until the next chat resolves it to a
SHA.

Status values: `todo` · `in progress` (uncommitted paths listed under Notes) · `done`.

| Phase | Note | Status | Commit | Eval log(s) | Notes for the next chat |
|---|---|---|---|---|---|
| 0 | 00 | done | c76777a | — | Platform facts E0.1–E0.5 are listed in note 01's Evals table and run first by phase 1's chat. `evals/sets/plan-phases.json` (evals 1–4) and `evals/sets/run-phase.json` (1–2) and their harness sheets are committed here. |
| 1 | 01 | done | 5fdfdd9 | `2026-09-19-run-evals-platform-facts.md`, `2026-09-19-run-evals-first-loop.md` | P1-B1 45/48 vs 12/48; the user reviewed the viewer and approved the commit; no `feedback.json` was received (no POST reached the server), so no written comments were acted on. `eval_workspace.py` also has `finalize` and `review` (see note 01 Deviations) — the loop needs both. *noticed:* `plan-phases.json` expectations name `chat.md`, which the executor prompt never asks for — graders were told to use transcript.md's final message; the grader critique (tie chart/chat checks to `proposal.html`, split "core stands without suggestions", add "stopped at approval") is listed in the first-loop log for the set's next edit. |
| 2 | 02 | done | 29b317b | `2026-09-19-trigger-sets-validate.md`, `2026-09-19-trigger-rates-automatic-skills.md` | P2-M1 held. Trigger rates: log-eval 0.95, build-site 1.00, new-plugin 1.00, run-evals 0.50 → 0.90 (description applied), check-contracts 0.70 → 1.00 — the optimizer's best dropped the scoping clause and guard (c) rejected it; a hand rewrite with the clause restored (user's request) scored 20/20 and was applied (note 02 Deviations). Guard (c) is "keeps the *current* scoping clause": only run-evals says `only inside a plugin's own subdirectory`; log-eval/build-site/check-contracts say "Use only in a plugin repo…", new-plugin has none — phase 7's Done-when checks should not assume the phrase. Trigger runs: scratch project in the session scratchpad, `PYTHONPATH=<sc>`, never from the skill-creator dir (see run-evals SKILL.md **Trigger evals**). *noticed:* run-evals' new description triggers 3/3 on a log-eval near-miss ("log that the implementer eval passed in dev-team/evals…") — rerun log-eval's trigger set with run-evals next time either changes |
| 3 | 03 | done | (phase 3) | `2026-09-19-plan-phases-evals-table.md` | P3-B1 47/48 vs 0.8.0's 47/48; P3-B2 7/7 vs 0/7. The user reviewed the viewer and asked for the P3-M1 fix; no written feedback was received. **`validate` no longer requires `target_path` to exist** (`init` still does), so phase-0 sets for targets not yet built validate; `run-evals/SKILL.md` **The set** says so (note 03 Deviations). The planted `smoke` still cannot fail the reader side of a `headings` claim: `cites` is literal; only owner-side unbolding fails it. Executors need `${CLAUDE_PLUGIN_ROOT}` mapped explicitly (worktree `plugin-dev/` for with_skill), or with_skill reads the installed templates. The static viewer served over `python3 -m http.server` works; the `review` server on :3117 accepted connections but never answered (a phase-1 Browser-pane tab was still open on it, likely the cause), so close old tabs first. *noticed:* the graders' critique of `plan-phases.json` is in the log, not applied (the note kept the set unchanged): 'Depends on: all' vs core-stands, two-way chart/table check, rendered width, 'stopped at approval', vacuous harness assertion. *noticed:* P3-B2's sets for typed commands had `harness: null`; plan-phases says a harness sheet is required only for a target that asks the user something. |
| 4 | 04 | todo | | | E0.5 held: a general-purpose subagent can spawn one more (its child wrote the file), so the executor can run `run-phase` nested. From phase 3: `validate` accepts a missing `target_path`; only `init` requires the target to exist. |
| 5 | 05 | todo | | | Suggested, accepted. Reuses P1-B1's iteration in `evals/workspace/plan-phases/`. |
| 6 | 06 | todo | | | Suggested, accepted. Touches `dev-team/` only; no dev-team bump. |
| 7 | 07 | todo | | | Ends with the 0.9.0 proposal in chat; `bump-version` runs only on a yes. From phase 2: only `run-evals`' description contains `only inside a plugin's own subdirectory` — don't check the other four for that phrase. |

## Standing facts

- Run every phase in Claude Code from the worktree's repo root with the plugin loaded from
  its working copy (`claude --plugin-dir ./plugin-dev`) — this plan changes the very skills
  that run it, so the installed plugin-dev (0.8.0) runs phase 1, and each later phase runs on
  the branch's own copy once `--plugin-dir` points at it.
- Every phase: edits → this plugin's `CLAUDE.md` rules → `check-contracts` → `build-site` →
  the phase's evals (from phase 2 on, through `run-evals`, stopping for review when a row is
  behavioral) logged with `log-eval` → one commit `plugin-dev 0.9-evals (phase N): …` → this
  file, in that commit.
- `evals/workspace/` is gitignored from phase 1 on; before that, keep workspaces out of the
  repo.
- Deviations from a note go under a `## Deviations` heading at the end of that note, in the
  same commit.
