# plugin-dev 0.9-evals — progress

Branch `plugin-dev-0.9-evals`. One row per phase of `0.9-evals-00-overview.md`. `run-phase`
reads this file first, does the first row that is not `done`, and fills the row in the same
commit as the phase. The Commit cell holds `(phase N)` until the next chat resolves it to a
SHA.

Status values: `todo` · `in progress` (uncommitted paths listed under Notes) · `done`.

| Phase | Note | Status | Commit | Eval log(s) | Notes for the next chat |
|---|---|---|---|---|---|
| 0 | 00 | done | (phase 0) | — | Platform facts E0.1–E0.5 are listed in note 01's Evals table and run first by phase 1's chat. `evals/sets/plan-phases.json` (evals 1–4) and `evals/sets/run-phase.json` (1–2) and their harness sheets are committed here. |
| 1 | 01 | todo | | | Worktree: `../cott-plugins-plugin-dev-0.9-evals`. P1-B1's baseline is the tag `plugin-dev-v0.7.0`. |
| 2 | 02 | todo | | | Needs E0.2's finding (write it here in phase 1). |
| 3 | 03 | todo | | | |
| 4 | 04 | todo | | | Needs E0.5's finding (write it here in phase 1). |
| 5 | 05 | todo | | | Suggested, accepted. Reuses P1-B1's iteration in `evals/workspace/plan-phases/`. |
| 6 | 06 | todo | | | Suggested, accepted. Touches `dev-team/` only; no dev-team bump. |
| 7 | 07 | todo | | | Ends with the 0.9.0 proposal in chat; `bump-version` runs only on a yes. |

## Standing facts

- Run every phase in Claude Code from the worktree's repo root with the plugin loaded from
  its working copy (`claude --plugin-dir ./plugin-dev`) — this plan changes the very skills
  that run it, so the installed plugin-dev (0.8.0) runs phase 1, and each later phase runs on
  the branch's own copy once `--plugin-dir` points at it.
- Every phase: edits → this plugin's `CLAUDE.md` rules → `check-contracts` → `build-site` →
  the phase's evals (from phase 2 on, through `run-evals`, stopping for review when a row is
  behavioral) logged with `log-eval` → one commit `plugin-dev 0.9-evals (phase N): …` → this
  file, in that commit.
- `evals/workspace/` is gitignored from phase 1 on; before that, keep workspaces out of the
  repo.
- Deviations from a note go under a `## Deviations` heading at the end of that note, in the
  same commit.
