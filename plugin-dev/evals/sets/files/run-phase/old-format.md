# Setup (the executor does this before anything else)
As in review-approve.md (same directory), with one change after copying and before the
first commit: delete the `## Evals` section (heading through the line before
`## Done when`) from site/notes/0.2-01-farewell.md. The note then names its eval only in
its Steps list, as notes written before plugin-dev 0.9 do.

# User's answers
- When the target stops for review of eval results: "Reviewed — approve, commit it."
- Anything else: the option marked Recommended.
