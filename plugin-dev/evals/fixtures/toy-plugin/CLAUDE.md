A fixture for plugin-dev's `run-phase` evals — not a real plugin; nothing here is published.
Its phases are done with `/plugin-dev:run-phase 0.2`, one chat and one commit each.
It has no `contracts.yml`, so `check-contracts` has nothing to check.
