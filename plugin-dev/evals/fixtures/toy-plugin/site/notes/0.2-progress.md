# toy 0.2 — progress

Branch `toy-0.2`. One row per phase of `0.2-00-overview.md`. `run-phase` reads this file
first, does the first row that is not `done`, and fills the row in the same commit as the
phase. The Commit cell holds `(phase N)` until the next chat resolves it to a SHA.

Status values: `todo` · `in progress` (uncommitted paths listed under Notes) · `done`.

| Phase | Note | Status | Commit | Eval log(s) | Notes for the next chat |
|---|---|---|---|---|---|
| 0 | 00 | done | fixture | — | |
| 1 | 01 | todo | | | |

## Standing facts

- Every phase: edits → the plugin's own rules → `check-contracts` → `build-site` → the
  phase's `## Evals` table run with `run-evals` and logged with `log-eval` → one commit
  `toy 0.2 (phase N): …` → this file, in that commit.
- Deviations from a note go under a `## Deviations` heading at the end of that note, in the
  same commit.
