# 01 — farewell

Phase 1. Adds the skill `farewell`, the mirror of `greet`: it writes a one-line goodbye.
The gap it closes: `toy` can greet someone but not say goodbye.

## Decisions

None — `farewell` copies `greet`'s shape exactly.

## Files

| Path | Change |
|---|---|
| `skills/farewell/SKILL.md` | new — verbatim below |

## Specification

`skills/farewell/SKILL.md`, verbatim:

```markdown
---
name: farewell
description: Say goodbye to someone by name. Use when the user asks to say goodbye to a person.
---

# Farewell

Write `outputs/farewell.txt` containing one line: `Goodbye, <name>.`
```

## Steps

1. Create `skills/farewell/SKILL.md` exactly as in **Specification**.
2. No `contracts.yml`, so no `check-contracts`; `site/` holds only these notes, so no
   `build-site`.
3. Evals: check that `skills/farewell/SKILL.md`'s frontmatter parses as YAML with `name:
   farewell` and a `description:`, then run `evals/sets/farewell.json` eval 1 (baseline
   none) with `run-evals`, and log both with `log-eval`.
4. Commit: `toy 0.2 (phase 1): farewell`.

## Evals

| ID | Kind | Target | Baseline | Set evals | Pass bar |
|---|---|---|---|---|---|
| T1-M1 | mechanical | `farewell` | — | — | `skills/farewell/SKILL.md`'s frontmatter parses as YAML with `name: farewell` and a non-empty `description:`. |
| T1-B1 | behavioral | `farewell` | none | `evals/sets/farewell.json` 1 | Every expectation passes for `with_skill`. |

## Done when

- `skills/farewell/SKILL.md` exists with the frontmatter above.
- T1-M1 passes; T1-B1 logged.
