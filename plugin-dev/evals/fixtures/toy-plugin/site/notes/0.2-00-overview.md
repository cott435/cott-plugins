# toy 0.2 — overview

Branch: `toy-0.2`. A fixture plan for plugin-dev's `run-phase` evals. Nothing here bumps or
tags anything.

## Why

`toy` can say hello (`greet`) but not goodbye. 0.2 adds `farewell`, the mirror of `greet`.

## Phases

One commit per phase. Commit messages begin `toy 0.2 (phase N): <what>`.

| Phase | Note | Commit contents | Depends on |
|---|---|---|---|
| 0 | 00 | this design set; `evals/sets/farewell.json` | — |
| 1 | 01 | `skills/farewell/SKILL.md` | 0 |

## Breaking changes to list in `CHANGELOG.md`

None.
